"""Manual research entry points (not part of the installed package or CI gate).

These scripts orchestrate I/O and printing over the library in `src/riskpremia`;
the testable logic lives in the library, so the scripts stay thin.
"""
